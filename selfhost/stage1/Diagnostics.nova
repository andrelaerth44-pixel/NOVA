fn empty() {
    return []
}

fn add(errors, message) {
    return array_push(errors, message)
}

fn format(errors) {
    out = ""
    for error in errors {
        out = out + "NOVA1: " + error + "\n"
    }
    return out
}
