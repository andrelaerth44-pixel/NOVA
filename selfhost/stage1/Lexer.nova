struct Token {
    kind: string
    text: string
}

fn make_token(kind, text) {
    return Token { kind: kind, text: text }
}

fn lex(source) {
    return [make_token("source", source), make_token("eof", "")]
}
